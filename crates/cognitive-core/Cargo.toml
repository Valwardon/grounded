[package]
name = "cognitive-core"
version = "0.1.0"
edition = "2024"
description = "Spreading activation daemon — background inference via energy propagation"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
parking_lot = { workspace = true }
bincode = { workspace = true }
semantic-graph = { path = "../semantic-graph" }
semantic-parser = { path = "../semantic-parser" }

